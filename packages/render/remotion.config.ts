// Remotion CLI / Studio config (programmatic rendering in Phase 7 uses
// @remotion/renderer directly and doesn't read this file).
import { Config } from "@remotion/cli/config";

Config.setVideoImageFormat("jpeg");
Config.setOverwriteOutput(true);
